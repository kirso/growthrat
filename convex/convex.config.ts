import { defineApp } from "convex/server";

const app = defineApp();

// Uncomment when @convex-dev/agent is set up:
// import agent from "@convex-dev/agent/convex.config.js";
// app.use(agent);

export default app;
